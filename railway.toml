[build]
root = "backend"           
builder = "NIXPACKS"

[deploy]
echo "web: pip install -r requirements.txt && uvicorn app.main:app --host 0.0.0.0 --port \$PORT" > backend/Procfile
healthcheckPath = "/health"

[build.nixpacks]
providers = ["python"]